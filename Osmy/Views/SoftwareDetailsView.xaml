<UserControl x:Class="Osmy.Views.SoftwareDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:local="clr-namespace:Osmy.Views"
             xmlns:vm="clr-namespace:Osmy.ViewModels"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="False"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vm:SoftwareDetailsViewViewModel}">
  <UserControl.Resources>
    <ScrollViewer x:Key="MainGrid">
      <ScrollViewer.Resources>
        <Style TargetType="ListView">
          <!--ListView上でマウスホイールによるスクロールを可能にする-->
          <Setter Property="Template">
            <Setter.Value>
              <ControlTemplate>
                <!--内部のScrollViewerを削除-->
                <!--<ScrollViewer>-->
                <ItemsPresenter/>
                <!--</ScrollViewer>-->
              </ControlTemplate>
            </Setter.Value>
          </Setter>
        </Style>
      </ScrollViewer.Resources>

      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.Resources>
          <Style TargetType="mah:MetroHeader" BasedOn="{StaticResource AccentMetroHeader}"/>
        </Grid.Resources>

        <mah:MetroHeader Grid.Row="0" Header="Name" Margin="0 0 0 5">
          <TextBlock Text="{Binding Software.Value.Name}"/>
        </mah:MetroHeader>

        <!--Latest Scan Result-->
        <mah:MetroHeader Grid.Row="1" Header="Latest Scan Result">
          <ListView ItemsSource="{Binding ScanResults.Value}" SelectionMode="Extended">
            <ListView.View>
              <GridView x:Name="_scanResultGridView" AllowsColumnReorder="False">
                <GridViewColumn>
                  <GridViewColumn.CellTemplate>
                    <DataTemplate>
                      <iconPacks:PackIconMaterial Kind="Bug" Foreground="Red">
                        <iconPacks:PackIconMaterial.Style>
                          <Style TargetType="iconPacks:PackIconMaterial">
                            <Setter Property="Visibility" Value="Hidden"/>
                            <Style.Triggers>
                              <DataTrigger Binding="{Binding IsVulnerable}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                              </DataTrigger>
                            </Style.Triggers>
                          </Style>
                        </iconPacks:PackIconMaterial.Style>

                        <iconPacks:PackIconMaterial.ToolTip>
                          <ItemsControl ItemsSource="{Binding VulnerabilityList.Vulnerabilities}">
                            <ItemsControl.ItemTemplate>
                              <DataTemplate>
                                <Grid>
                                  <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                  </Grid.RowDefinitions>

                                  <mah:MetroHeader Grid.Row="0" Header="Summary">
                                    <TextBlock Text="{Binding Summary}"/>
                                  </mah:MetroHeader>

                                  <mah:MetroHeader Grid.Row="1" Header="Severity">
                                    <ItemsControl ItemsSource="{Binding Severity}">
                                      <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                          <TextBlock>
                                      [<Run Text="{Binding Type}"/>] <Run Text="{Binding Score}"/>
                                          </TextBlock>
                                        </DataTemplate>
                                      </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                  </mah:MetroHeader>
                                </Grid>
                              </DataTemplate>
                            </ItemsControl.ItemTemplate>
                          </ItemsControl>
                        </iconPacks:PackIconMaterial.ToolTip>
                      </iconPacks:PackIconMaterial>
                    </DataTemplate>
                  </GridViewColumn.CellTemplate>
                </GridViewColumn>
                <GridViewColumn Header="Name">
                  <GridViewColumn.CellTemplate>
                    <DataTemplate>
                      <TextBlock Text="{Binding Package.Name}">
                        <TextBlock.Style>
                          <Style TargetType="TextBlock" BasedOn="{StaticResource MahApps.Styles.TextBlock}">
                            <Setter Property="FontWeight" Value="Normal"/>
                            <Style.Triggers>
                              <DataTrigger Binding="{Binding Package.IsRootPackage}" Value="True">
                                <Setter Property="FontWeight" Value="Bold"/>
                              </DataTrigger>
                            </Style.Triggers>
                          </Style>
                        </TextBlock.Style>
                      </TextBlock>
                    </DataTemplate>
                  </GridViewColumn.CellTemplate>
                </GridViewColumn>
                <GridViewColumn Header="Version" DisplayMemberBinding="{Binding Package.Version}"/>
              </GridView>
            </ListView.View>

            <ListView.Resources>
              <Style TargetType="ListViewItem" BasedOn="{StaticResource MahApps.Styles.ListViewItem}">
                <Setter Property="Background" Value="Transparent"/>
                <Style.Triggers>
                  <DataTrigger Binding="{Binding IsVulnerable}" Value="True">
                    <Setter Property="Background" Value="Pink"/>
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </ListView.Resources>
          </ListView>
        </mah:MetroHeader>

        <!--SBOM-->
        <mah:MetroHeader Grid.Row="2" Header="SBOMs" Margin="0 5 0 0">
          <ListView ItemsSource="{Binding Software.Value.Sboms}" SelectionMode="Single">
            <ListView.View>
              <GridView AllowsColumnReorder="False">
                <GridViewColumn>
                  <GridViewColumn.CellTemplate>
                    <DataTemplate>
                      <iconPacks:PackIconMaterial Kind="CheckBold">
                        <iconPacks:PackIconMaterial.Style>
                          <Style TargetType="iconPacks:PackIconMaterial">
                            <Setter Property="Visibility" Value="Hidden"/>
                            <Style.Triggers>
                              <DataTrigger Binding="{Binding IsUsing}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                              </DataTrigger>
                            </Style.Triggers>
                          </Style>
                        </iconPacks:PackIconMaterial.Style>
                      </iconPacks:PackIconMaterial>
                    </DataTemplate>
                  </GridViewColumn.CellTemplate>
                </GridViewColumn>
                <GridViewColumn Header="Software Name" DisplayMemberBinding="{Binding RootPackage.Name}"/>
                <GridViewColumn Header="Software Version" DisplayMemberBinding="{Binding RootPackage.Version}"/>
              </GridView>
            </ListView.View>
          </ListView>
        </mah:MetroHeader>

        <!--Files-->
        <mah:MetroHeader Grid.Row="3" Header="Files" Margin="0 5 0 0">
          <ListView ItemsSource="{Binding HashValidationResults.Value}" SelectionMode="Single">
            <ListView.View>
              <GridView AllowsColumnReorder="False">
                <GridViewColumn Header="Name" DisplayMemberBinding="{Binding SbomFile.FileName}"/>
                <GridViewColumn Header="Result">
                  <GridViewColumn.CellTemplate>
                    <DataTemplate>
                      <iconPacks:PackIconMaterial Kind="Alert" Foreground="Red">
                        <iconPacks:PackIconMaterial.Style>
                          <Style TargetType="iconPacks:PackIconMaterial">
                            <Setter Property="Visibility" Value="Hidden"/>
                            <Style.Triggers>
                              <DataTrigger Binding="{Binding IsValid}" Value="False">
                                <Setter Property="Visibility" Value="Visible"/>
                              </DataTrigger>
                            </Style.Triggers>
                          </Style>
                        </iconPacks:PackIconMaterial.Style>
                      </iconPacks:PackIconMaterial>
                    </DataTemplate>
                  </GridViewColumn.CellTemplate>
                </GridViewColumn>
                <GridViewColumn Header="Checksum" Width="600">
                  <GridViewColumn.CellTemplate>
                    <DataTemplate>
                      <ItemsControl x:Name="checksums" ItemsSource="{Binding SbomFile.Checksums}">
                        <ItemsControl.ItemTemplate>
                          <DataTemplate>
                            <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="2" Padding="5 0" Margin="0 0 3 0"
                                    HorizontalAlignment="Left" Width="60" ToolTip="{Binding Value}" Background="Transparent">
                              <TextBlock Text="{Binding Algorithm}" HorizontalAlignment="Center"/>
                            </Border>
                          </DataTemplate>
                        </ItemsControl.ItemTemplate>

                        <ItemsControl.ItemsPanel>
                          <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" IsItemsHost="True"/>
                          </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                      </ItemsControl>
                    </DataTemplate>
                  </GridViewColumn.CellTemplate>
                </GridViewColumn>
              </GridView>
            </ListView.View>
          </ListView>
        </mah:MetroHeader>

        <StackPanel Grid.Row="4" Height="30" Margin="0 0 5 5" 
                HorizontalAlignment="Right" Orientation="Horizontal">
          <Button ToolTip="Add Software" Command="{Binding AddSbomCommand}">
            <iconPacks:PackIconMaterial Kind="Plus"/>
          </Button>
        </StackPanel>
      </Grid>
    </ScrollViewer>

    <Grid x:Key="NotSelectedGrid" Background="LightGray">
      <TextBlock Text="Software is not selected" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
  </UserControl.Resources>

  <ContentControl>
    <ContentControl.Style>
      <Style TargetType="ContentControl">
        <Setter Property="Content" Value="{StaticResource MainGrid}"/>
        <Style.Triggers>
          <Trigger Property="DataContext" Value="{x:Null}">
            <Setter Property="Content" Value="{StaticResource NotSelectedGrid}"/>
          </Trigger>
        </Style.Triggers>
      </Style>
    </ContentControl.Style>
  </ContentControl>
</UserControl>
