<UserControl x:Class="Osmy.Views.SbomListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             xmlns:local="clr-namespace:Osmy.Views"
             xmlns:controls="clr-namespace:Osmy.Controls"
             xmlns:viewmodels="clr-namespace:Osmy.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="700"
             d:DataContext="{d:DesignInstance Type=viewmodels:SbomListViewViewModel}">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="1*" MinWidth="200"/>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition Width="2*" MinWidth="200"/>
    </Grid.ColumnDefinitions>

    <Grid Grid.Column="0" Margin="5">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <mah:MetroHeader Grid.Row="0" Header="SBOMs" Style="{StaticResource AccentMetroHeader}" VerticalAlignment="Center"/>
      <ListBox Grid.Row="1" ItemsSource="{Binding SbomInfos.Value}"
               SelectedItem="{Binding SelectedSbomInfo.Value}" SelectionMode="Single">
        <ListBox.ItemTemplate>
          <DataTemplate>
            <controls:SbomInfoPresenter Value="{Binding}"/>
          </DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>

      <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
        <Button ToolTip="Add SBOM" Height="30"
                Command="{Binding AddSbomCommand}">
          <StackPanel Orientation="Horizontal">
            <iconPacks:PackIconMaterial Kind="Plus"/>
            <TextBlock Text="Add" Margin="5 0 0 0"/>
          </StackPanel>
        </Button>

        <Button ToolTip="Remove SBOM" Height="30" Margin="5 0 0 0"
                Command="{Binding DeleteSbomCommand}">
          <StackPanel Orientation="Horizontal">
            <iconPacks:PackIconMaterial Kind="TrashCan"/>
            <TextBlock Text="Remove" Margin="5 0 0 0"/>
          </StackPanel>
        </Button>

        <!--<Button ToolTip="Scan" Content="Scan" Command="{Binding ScanVulnsCommand}"/>-->
      </StackPanel>
    </Grid>

    <GridSplitter Grid.Column="1" VerticalAlignment="Stretch" Width="5" Margin="0 5"/>

    <local:SbomDetailsView Grid.Column="2" Margin="5" DataContext="{Binding SelectedSbomVM.Value}"/>

    <!--<TextBlock Grid.Column="2" Margin="5" Text="{Binding LatestScanResult.Value}"/>-->
  </Grid>
</UserControl>
